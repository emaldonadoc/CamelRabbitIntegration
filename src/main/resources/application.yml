spring.main.sources: com.railtronix.traker.camel.TrakingRoute

rest:
  host: localhost
  port: 8080

rabbitmq:
  host: localhost
  port: 5672
  exchange: rtx
  queueTrack: rtx-tracker


spring:
    data:
        mongodb:
            host: localhost
            port: 27017
            database: rtx_tracker
ampq:
    broker:
        queue.name: rtx-tracker
        exchange.name: rtx
